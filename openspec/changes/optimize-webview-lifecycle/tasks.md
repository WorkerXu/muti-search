## 1. 测试

- [x] 1.1 增加测试，确认启动时不会急切导航所有 WebView。
- [x] 1.2 增加测试，确认已选搜索服务会在发送前加载。
- [x] 1.3 增加测试，确认未选服务不会因为搜索发送而加载。
- [x] 1.4 增加测试，确认切换 Tab 后释放非活跃工作流 WebView。
- [x] 1.5 增加测试，确认释放 WebView 后代码问答历史仍可导出。

## 2. 生命周期模型

- [x] 2.1 增加 WebView 生命周期状态：unloaded、loading、ready、warm、released 和 error。
- [x] 2.2 增加创建、导航、预热和释放 WebView 的 helper。
- [x] 2.3 确保持久化 partition 在首次导航前设置。
- [x] 2.4 增加 generation guard，避免释放逻辑打断正在进行的发送。

## 3. 搜索工作流集成

- [x] 3.1 延迟搜索 WebView 的 `src` 设置，直到服务被查看或被选中发送。
- [x] 3.2 确保已选服务在搜索发送脚本执行前完成加载。
- [x] 3.3 为未加载和加载中的服务提供清晰 UI 状态。

## 4. 代码工作流集成

- [x] 4.1 用户连续追问当前仓库时，保持当前仓库代码页预热。
- [x] 4.2 当代码工作流非活跃且没有待处理发送时，释放代码站点 WebView。
- [x] 4.3 需要时从仓库状态恢复代码站点页面。

## 5. 验证

- [x] 5.1 Run `npm run typecheck`.
- [x] 5.2 Run `npm test`.
- [x] 5.3 Run `npm run build`.
- [x] 5.4 对比生命周期变更前后的启动 WebView 导航行为。
