{
  "name": "muti-search",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main/main.js",
  "scripts": {
    "dev": "concurrently -k \"tsc -p tsconfig.node.json --watch --preserveWatchOutput\" \"vite --host 127.0.0.1\" \"wait-on dist/main/main.js http://127.0.0.1:5173 && nodemon --watch dist/main --watch dist/preload --ext js --exec \\\"VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .\\\"\"",
    "build": "npm run build:main && vite build",
    "build:main": "tsc -p tsconfig.node.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.node.json --noEmit"
  },
  "dependencies": {
    "electron": "^39.2.6"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "concurrently": "^9.2.1",
    "jsdom": "^27.2.0",
    "nodemon": "^3.1.10",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vitest": "^4.1.6",
    "wait-on": "^9.0.3"
  }
}
